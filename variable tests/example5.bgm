# Loop : should they be prevented?

%active User : 1;
%active Room : 0;

%name default;

User[@a] -> User[default];

User[default];

# User[@b]; -> This gives the same result since variables are allowed inside the model!


%check;
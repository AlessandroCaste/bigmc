# Example of 'bifurcation'

%active User : 1;
%passive Room : 0;
%active Change : 1;

%name default;


Room.User[@a] -> User[@a];
Room.User[default] -> Change[default];

# A model definition with an unexpected name will work instead, since the new name will be bound
Room.User[Alfred] | Room.User[default];

%check;

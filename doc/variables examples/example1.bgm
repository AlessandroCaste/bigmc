# Example of multiple matching

%active User : 1;
%passive Room : 0;

%name default;

# A reaction written like this won't work since a is not a variable nor an already declared name
# Room.User[a] -> Room.User[a];

Room.User[@a] -> User[@a];

# A model definition with an unexpected name will work instead, since the new name will be bound
Room.User[Alfred] | Room.User[Bruce];

%check;

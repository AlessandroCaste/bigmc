# Example of bifurcation

%active User : 1;
%passive Room : 0;
%active Change : 1;

%name default;


Room.User[@a] -> User[@a];
Room.User[default] -> Change[default];

Room.User[Alfred] | Room.User[default];

%check;

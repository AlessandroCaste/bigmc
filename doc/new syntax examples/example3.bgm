control:
    passive send  1
    passive recv  1

names:
    outer a
    outer b

rule a: send[x].$0 | recv[x].$1 -> $0 | $1

send[a].recv[b] | recv[a].send[b]


#properties: 
#    finished !empty();
#    eq !equal(send);

check
